/* Requires the Docker Pipeline plugin */
pipeline {
    agent { label workers }
    stages {
        stage('build') {
            steps {
                sh 'python --version'
            }
        }
    }
}